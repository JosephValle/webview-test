name: webview_test
description: "A new Web View Test project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.6.1

dependencies:
  archive: ^4.0.3
  connectivity_plus: ^6.1.3
  cupertino_icons: ^1.0.8
  flutter:
    sdk: flutter
  flutter_inappwebview: ^6.1.5
  markdown_widget: ^2.3.2+6
  path_provider: ^2.1.5
  permission_handler: ^11.3.1

dev_dependencies:
  flutter_lints: ^5.0.0
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
  assets:
    - assets/left_hand_tap.png
